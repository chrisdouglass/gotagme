syntax = "proto3";

package huskysoft.gotagme.common;

enum ApprovalState {
  NEW = 0;
  APPROVED = 1;
  REJECTED = 2;
}

message ApprovalStatus {
  ApprovalState state = 1;
  User setBy = 2;
  int64 createdAt = 3;
}
